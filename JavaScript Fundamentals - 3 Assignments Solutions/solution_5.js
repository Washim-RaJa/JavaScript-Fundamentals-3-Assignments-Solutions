function calculateCircularArea(r){
    let circularArea = (Math.PI)*(Math.pow(r,2));
    console.log(`The area of the circle is ${circularArea} sq. unit`);
}

// Enter radius value
calculateCircularArea(7);